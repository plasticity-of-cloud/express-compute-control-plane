apiVersion: v1
kind: ServiceAccount
metadata:
  name: eks-dx-pod-identity-webhook
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: eks-dx-pod-identity-webhook
rules:
- apiGroups: ["eks.plasticity.cloud"]
  resources: ["podidentityassociations"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: eks-dx-pod-identity-webhook
subjects:
- kind: ServiceAccount
  name: eks-dx-pod-identity-webhook
  namespace: kube-system
roleRef:
  kind: ClusterRole
  name: eks-dx-pod-identity-webhook
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-dx-pod-identity-webhook
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eks-dx-pod-identity-webhook
  template:
    metadata:
      labels:
        app: eks-dx-pod-identity-webhook
    spec:
      serviceAccountName: eks-dx-pod-identity-webhook
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: eks-dx-pod-identity-webhook
        image: 864899852480.dkr.ecr.ap-south-1.amazonaws.com/plcloud/eks-dx-pod-identity-webhook:latest
        ports:
        - containerPort: 8443
          name: https
        - containerPort: 8080
          name: http
        env:
        - name: EKS_CLUSTER_NAME
          value: "my-cluster"
        volumeMounts:
        - name: tls-certs
          mountPath: /tls
          readOnly: true
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 5
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 5
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
      volumes:
      - name: tls-certs
        secret:
          secretName: eks-dx-pod-identity-webhook-tls
---
apiVersion: v1
kind: Service
metadata:
  name: eks-dx-pod-identity-webhook
  namespace: kube-system
spec:
  selector:
    app: eks-dx-pod-identity-webhook
  ports:
  - name: https
    port: 8443
    targetPort: 8443
