name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - 'LICENSE'
      - '.gitignore'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - 'LICENSE'
      - '.gitignore'

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Build and test (all modules)
        env:
          AWS_REGION: us-east-1
        run: mvn -B verify -DskipITs

      - name: Build Lambda function.zips
        run: |
          mvn -B -pl ecp-model,ecp-credential-service package -DskipTests
          mvn -B -pl ecp-model,ecp-mgmt-service package -DskipTests
          mvn -B -pl ecp-model,ecp-tenant-service package -DskipTests

      - name: Synth CDK stack
        run: mvn -B -pl infra compile exec:java -Ddevelopment=true

      - name: Build container images (JVM, no push)
        run: |
          mvn -B -pl ecp-auth-proxy package -DskipTests \
            -Dquarkus.container-image.build=true \
            -Dquarkus.container-image.push=false
          mvn -B -pl ecp-workload-identity-webhook package -DskipTests \
            -Dquarkus.container-image.build=true \
            -Dquarkus.container-image.push=false

      - name: Lint Helm charts
        run: |
          helm lint ecp-auth-proxy/target/helm/kubernetes/express-compute-auth-proxy
          helm lint ecp-workload-identity-webhook/target/helm/kubernetes/express-compute-workload-identity-webhook

  integration-test:
    runs-on: ubuntu-24.04
    services:
      dynamodb-local:
        image: public.ecr.aws/aws-dynamodb-local/aws-dynamodb-local:latest
        ports:
          - 18000:8000
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Run DynamoDB integration tests
        env:
          AWS_REGION: us-east-1
        run: |
          mvn -B -pl ecp-model,ecp-mgmt-service test \
            -Dtest=DynamoDbIntegrationTest \
            -Dsurefire.failIfNoSpecifiedTests=false \
            -Dintegration.dynamodb=true \
            -Ddynamodb.endpoint=http://localhost:18000
