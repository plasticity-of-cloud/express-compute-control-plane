# Application configuration
quarkus.application.name=aws-eks-auth-service-proxy
quarkus.application.version=1.0.0

# HTTP configuration
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# Native build configuration
quarkus.native.container-build=true
quarkus.native.builder-image=quay.io/quarkus/ubi-quarkus-graalvmce-builder-image:jdk-21

# Health checks
quarkus.smallrye-health.root-path=/health

# Metrics
quarkus.micrometer.export.prometheus.enabled=true
quarkus.micrometer.export.prometheus.path=/metrics

# OpenAPI
quarkus.smallrye-openapi.path=/openapi
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.plcloud".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n

# EKS Pod Identity configuration
eks.pod-identity.configmap.name=pod-identity-associations
eks.pod-identity.configmap.namespace=kube-system

# AWS STS configuration
aws.sts.session-duration=PT1H

# Kubernetes client configuration
quarkus.kubernetes-client.trust-certs=true

# JWT / OIDC validation (EKS Distro kube-apiserver as OIDC provider)
mp.jwt.verify.issuer=https://kubernetes.default.svc
mp.jwt.verify.publickey.location=https://kubernetes.default.svc/openid/v1/jwks
mp.jwt.verify.audiences=pods.eks.amazonaws.com

# Trust cluster self-signed cert (replace with CA mount in production)
quarkus.tls.trust-all=true

# Native build specific configuration
quarkus.native.additional-build-args=--initialize-at-build-time=org.slf4j.LoggerFactory,org.slf4j.impl.StaticLoggerBinder,--initialize-at-run-time=io.fabric8.kubernetes.client.utils.Utils

# Picocli configuration
quarkus.picocli.native-image-options=-H:+ReportExceptionStackTraces

# Container image (Jib)
quarkus.container-image.group=plcloud
quarkus.container-image.name=aws-eks-auth-service-proxy
quarkus.container-image.tag=latest
quarkus.native.builder-image=quay.io/quarkus/ubi-quarkus-graalvmce-builder-image:jdk-21
quarkus.jib.base-native-image=quay.io/quarkus/quarkus-distroless-image:2.0

# Kubernetes manifest generation
quarkus.kubernetes.namespace=kube-system
quarkus.kubernetes.service-type=ClusterIP
quarkus.kubernetes.replicas=1
quarkus.kubernetes.ports."http".container-port=8080
quarkus.kubernetes.liveness-probe.http-action-path=/health/live
quarkus.kubernetes.readiness-probe.http-action-path=/health/ready
quarkus.kubernetes.env.secrets=aws-credentials
quarkus.kubernetes.service-account=eks-auth-proxy
